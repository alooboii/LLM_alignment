# Requirements for Alignment Methods Assignment - Kaggle Optimized
# Python 3.8+ required

# ============================================================================
# Core Deep Learning Frameworks (Kaggle pre-installed, but pinning versions)
# ============================================================================
torch>=2.0.0
# torchvision and torchaudio not needed for this project

# ============================================================================
# Transformers & LLM Libraries
# ============================================================================
transformers>=4.36.0
datasets>=2.14.0
tokenizers>=0.15.0
accelerate>=0.25.0
peft>=0.7.0
trl>=0.7.10
bitsandbytes>=0.41.0  # For 4-bit/8-bit quantization

# ============================================================================
# Scientific Computing (Most pre-installed on Kaggle)
# ============================================================================
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# ============================================================================
# Visualization (Pre-installed on Kaggle)
# ============================================================================
matplotlib>=3.7.0
seaborn>=0.12.0

# ============================================================================
# Machine Learning & Metrics (Pre-installed on Kaggle)
# ============================================================================
scikit-learn>=1.3.0

# ============================================================================
# Utilities
# ============================================================================
tqdm>=4.65.0
# requests and aiohttp pre-installed on Kaggle

# ============================================================================
# Data Processing (Pre-installed on Kaggle)
# ============================================================================
pyarrow>=12.0.0
huggingface-hub>=0.19.0

# ============================================================================
# KAGGLE-SPECIFIC NOTES
# ============================================================================
# The following are already installed on Kaggle and don't need pip install:
# - numpy, pandas, scipy, scikit-learn
# - matplotlib, seaborn
# - torch (with CUDA)
# - jupyter, ipywidgets
# - requests, tqdm
#
# For Kaggle, you only need to install:
# !pip install transformers datasets accelerate peft trl bitsandbytes
#
# Or for minimal install:
# !pip install -q transformers datasets accelerate peft trl bitsandbytes --upgrade